{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/go-wasm-srv",
            "cwd": "${workspaceFolder}/go-wasm-srv"
        }
    ],
    "compounds": []
}